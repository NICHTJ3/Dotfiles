- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
    # Karabiner config
    ~/.config/karabiner/karabiner.json: ./config/karabiner.json
