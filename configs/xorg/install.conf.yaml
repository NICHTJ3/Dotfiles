- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
    # X config
    ~/.xinitrc: config/xinitrc
    ~/.xprofile: config/xprofile
    ~/.config/xprofile: config/xprofile
    ~/.Xresources: config/Xresources
